// 测试多个类型参数的 trait bound

trait Show {
    fun show(self): str
}

type Pair<A: Show, B: Show> = Pair(first: A, second: B) {
    fun show_pair(self): str {
        "(" + self.first.show() + ", " + self.second.show() + ")"
    }
}

fun main() {
    println("Multi-param trait bound test")
}
