// Benchmark 1: 非尾递归斐波那契（强化版）
// 目标：压测函数调用栈、递归、整数算术
// 负载：fib(32)，约 700 万次非尾递归调用

import builtin.io.Console.{println}

fun fib(n: i64): i64 {
    if n < 2 { n } else { fib(n - 1) + fib(n - 2) }
}

fun main(): void {
    println(fib(32))
}
